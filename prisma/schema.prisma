datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id          String   @id @default(cuid())
  title       String
  description String?
  type        String
  date        DateTime
  time        DateTime
  location    String?
}
